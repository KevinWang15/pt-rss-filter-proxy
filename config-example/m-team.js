module.exports = {
    cookie: "tp=NT****",

    // only show freeleech torrents
    freeleech: true,

    // in MB, only show torrents with size less than 5000MB (0 for unlimited)
    fileSize: 5000,

};